# Commands used to install or setup new environment

# Install chocolatey - Instructions found here: https://chocolatey.org/install

# Install OpenSSH using current version (Not one packaged with windows)
choco install openssh -params "/SSHServerFeature /SSHAgentFeature"

# Install FiraCode font
choco install FiraCode
